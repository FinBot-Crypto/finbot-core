# =============================================================
# Finanças Crypto Bot — Variáveis de Infraestrutura (EXEMPLO)
# =============================================================
# Copie para .env e configure:
#   cp .env.example .env && nano .env
# =============================================================

# PostgreSQL
POSTGRES_USER=crypto_admin
POSTGRES_PASSWORD=TROCAR_PARA_SENHA_FORTE
POSTGRES_DB=crypto_bot
POSTGRES_PORT=5432

# NATS (mensageria + KV store)
NATS_PORT=4222
NATS_MONITOR_PORT=8222
